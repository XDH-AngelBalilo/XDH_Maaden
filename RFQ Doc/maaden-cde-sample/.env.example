# Maaden CDE sample — copy to .env.local
DATABASE_URL=postgresql://cde:cde_local_dev@localhost:5432/maaden_cde
JWT_SECRET=dev-only-change-me

# Phase 2 (AI) — optional; app must run without it
ANTHROPIC_API_KEY=
